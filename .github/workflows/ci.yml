name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  MIX_ENV: test
  OTP_VERSION: "26.2.5.20"
  ELIXIR_VERSION: "1.18.4"

permissions:
  contents: read

jobs:
  ci:
    name: OTP 26.2.5.20 / Elixir 1.18.4
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ env.OTP_VERSION }}
          elixir-version: ${{ env.ELIXIR_VERSION }}

      - name: Cache deps and _build (incl. dialyzer PLTs)
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: mix-${{ env.OTP_VERSION }}-${{ env.ELIXIR_VERSION }}-${{ hashFiles('mix.lock') }}
          restore-keys: |
            mix-${{ env.OTP_VERSION }}-${{ env.ELIXIR_VERSION }}-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile
        run: mix compile

      - name: Format
        run: mix format --check-formatted

      - name: Test
        run: mix coveralls.github --warnings-as-errors
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Credo
        run: mix credo --strict

      - name: Dialyzer
        run: mix dialyzer
